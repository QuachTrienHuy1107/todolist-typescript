import React from "react"

const NotFound: React.FC<Record<string, never>> = () => (
    <div>
        <h1 className="text-dark display-4 text-center">PAGE NOT FOUND</h1>
    </div>
)
export default NotFound
